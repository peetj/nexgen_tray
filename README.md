# nexgen_tray

Single systray hub for all Nexgen utilities.

Planned responsibilities:
- Own all global hotkeys (e.g. Ctrl+Alt+T for clock)
- Launch utilities as separate EXEs when needed
- Control utilities over IPC (QLocalSocket JSON)
- Central settings (`QSettings("Nexgen","Utilities")`)
